import styled from 'styled-components';

const HomeContainer = styled('div')`
  ul {
    display: flex;
    flex-wrap: wrap;
    list-style: none;
  }

  ul li {
    flex: 0 auto;
  }
`;

export default HomeContainer;
